# Base de datos (RDS PostgreSQL en produccion)
DATABASE_URL=postgres://lean:lean@localhost:5432/leanshop

# JWT
JWT_SECRET=lean-shop-dev-secret-key-change-in-production

# AWS S3 (opcional - si no se configura, usa disco local)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=
